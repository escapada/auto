<% content_for :body do %>
<h1>СЕЙЧАС РАЗБИРАЕМ</h1>
<% @items.each do |item| %>
<div class="item_prw">
	<% item.photos.each do |photo| %>
		<%= photo.main ? image_tag(photo.image.url(:thumb)) : "smt" %>
		<%#= image_tag(photo.image.url(:thumb)) if photo.main %>
		<%#= image_tag(photo.image.url(:thumb)) if photo.main %>

	<% end %>
	<%#=image_tag(item.photos.image.url(:thumb))%>
	<div class="item_info">
		<%#=image_tag(item.image.url(:main))%>
		<div class="item_txt">

		</div>
	</div>
</div>

<% end %>
		<%= @items.count%>
<% end %>
	


	<%#= link_to item.title, refinery.items_item_path(item) %>
        <%#= link_to item.title, refinery.items_item_path(item) %>

<%= render '/refinery/content_page' %>
