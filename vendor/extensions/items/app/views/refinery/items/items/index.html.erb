<% content_for :body do %>
	<h1>СЕЙЧАС РАЗБИРАЕМ</h1>
	<div class="topmargin">
	<% @items.each do |item| %>
	<div class='<%=cycle("item_prw odd", "item_prw even")%>' >
		<% item.photos.each do |photo| %>
			<%= link_to image_tag(photo.image.url(:medium)), refinery.items_item_path(item) if photo.main %>
		<% end %>
		<div class="item_info">
			<%=image_tag(item.model.downcase+'.png')%>
			<div class="items_txt">
				<%= item.title %>
			</div>
		</div>
	</div>
	<% end %>
	</div>


<% end %>

<% content_for :side_body do %>
	<h1>СЕЙЧАС</h1>
<% end %>
	


	<%#= link_to item.title, refinery.items_item_path(item) %>
        <%#= link_to item.title, refinery.items_item_path(item) %>

<%= render '/refinery/content_page' %>
