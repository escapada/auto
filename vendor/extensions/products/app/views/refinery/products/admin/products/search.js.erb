var txt = '';
<% @results.each do |element| %>
 txt += '<div class="add_carmodel_to_product" onclick="addmodeltoproduct(\'<%=@model_type%>\', <%=element.id%>, \'<%= element.title %>\')"><%=element.title%></div>';
<% end %>
$('#<%=@model_type%> + .tmplistitems').html(txt);

